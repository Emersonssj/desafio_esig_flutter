name: desafio_esig_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  mobx: ^2.6.0
  flutter_mobx: ^2.3.0
  get_it: ^9.2.1
  flutter_dotenv: ^6.0.0
  result_dart: ^1.1.0
  shared_preferences: ^2.5.4
  image_picker: ^1.2.1
  photo_manager: ^3.9.0
  photo_manager_image_provider: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  mobx_codegen: ^2.6.0
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - .env